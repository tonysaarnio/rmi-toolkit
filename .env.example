SF_TARGET_ORG=your-org-alias
# Only needed on an org older than 264 — set to the org's own max API version
# (e.g. v67.0 for Summer '26). Defaults to v68.0; a too-new value makes every
# invoice fail with HTTP 404 while orders still activate. Floor is v62.0.
#RMI_API_VERSION=v67.0
